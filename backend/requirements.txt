# --- Phase 1 core -----------------------------------------------------------
# Dependencies are added phase by phase rather than all at once, so that a
# broken build always points at the phase that introduced it.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
python-dotenv==1.0.1
httpx==0.28.1

# --- Phase 2 auth ---------------------------------------------------------
# PyJWT over python-jose: actively maintained and no dependency on the
# unmaintained ecdsa package.
PyJWT==2.10.1
# passlib 1.7.4 reads bcrypt.__about__, which bcrypt >= 4.1 removed. Pinning
# bcrypt to 4.0.1 keeps the pair warning-free; revisit when passlib 1.8 lands.
passlib==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
python-multipart==0.0.20

# --- Phase 5 extraction and matching --------------------------------------
rapidfuzz==3.10.1
scikit-learn==1.6.0

# Optional: true sentence embeddings. Not installed by default because torch
# adds ~1-2 GB to the image. Enable by installing it and setting
# EMBEDDING_PROVIDER=sentence_transformer.
#   pip install sentence-transformers==3.3.1
# The matcher falls back to the lexical TF-IDF provider when it is absent.

# --- Testing ----------------------------------------------------------------
pytest==8.3.4
pytest-cov==6.0.0

# --- Added in later phases --------------------------------------------------
# Phase 3 : pandas, openpyxl
pandas==2.2.3
openpyxl==3.1.5
# Phase 4 : document processing and background work
PyMuPDF==1.25.1
pdfplumber==0.11.5
celery[redis]==5.4.0
redis==5.2.1
# Phase 7 : scikit-learn, joblib
# Phase 8 : reportlab / weasyprint
