# --- Phase 1 core -----------------------------------------------------------
# Dependencies are added phase by phase rather than all at once, so that a
# broken build always points at the phase that introduced it.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
python-dotenv==1.0.1
httpx==0.28.1

# --- Phase 2 auth ---------------------------------------------------------
# PyJWT over python-jose: actively maintained and no dependency on the
# unmaintained ecdsa package.
PyJWT==2.10.1
# passlib 1.7.4 reads bcrypt.__about__, which bcrypt >= 4.1 removed. Pinning
# bcrypt to 4.0.1 keeps the pair warning-free; revisit when passlib 1.8 lands.
passlib==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
python-multipart==0.0.20

# --- Testing ----------------------------------------------------------------
pytest==8.3.4
pytest-cov==6.0.0

# --- Added in later phases --------------------------------------------------
# Phase 3 : pandas, openpyxl
# Phase 4 : pymupdf, pdfplumber, celery, redis, python-multipart
# Phase 5 : rapidfuzz, sentence-transformers
# Phase 7 : scikit-learn, joblib
# Phase 8 : reportlab / weasyprint
